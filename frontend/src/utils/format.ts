// =============================================================
// 段功能：前端通用格式化工具（M2）
// 说明：集中处理金额等展示格式，避免各页面重复实现。
//       金额按中文习惯使用 ¥ 符号 + 千位分隔（与后台 guide_price 单位“元”一致）。
// =============================================================

/**
 * 格式化金额（元）为中文展示：¥899,000
 * @param v 数值金额（元）
 */
export function formatPrice(v: number): string {
  return "¥" + v.toLocaleString("zh-CN");
}
