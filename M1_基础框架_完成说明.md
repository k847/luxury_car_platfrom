# M1 基础框架（脚手架 + 数据库 + 鉴权）完成说明

> 依据：《开发技术文档》§14 M1、《数据库设计文档》（29 张表）
> 状态：代码已完成并验证，待用户确认后进入 M2

---

## 一、后端 `backend/`（FastAPI + SQLAlchemy 2.x + MySQL）

| 文件 | 段功能 |
| --- | --- |
| `requirements.txt` | 锁定运行依赖（FastAPI / Uvicorn / SQLAlchemy / PyMySQL / Alembic / Pydantic v2 / passlib[bcrypt] / PyJWT 等），每项带中文注释 |
| `app/core/config.py` | 配置中心：数据库 URL、JWT 密钥与过期时长、CORS 来源、限流参数（读 `.env`） |
| `app/core/database.py` | 数据库引擎 / SessionLocal / Base（依赖注入 `get_db`） |
| `app/core/security.py` | 安全工具：bcrypt 密码哈希、JWT 签发（access/refresh 带 `type` 声明）、token 解码校验 |
| `app/core/deps.py` | 鉴权依赖：`get_current_user` 解析 JWT 并加载当前用户；`require_permission` 工厂实现 RBAC 权限校验 |
| `app/models.py` | ORM 模型：29 张表（22 主表 + 7 `*_i18n` 翻译表）；`CommonColumns`/`I18nColumns` 公共字段 mixin；列注释与文档 DDL 一一对应 |
| `app/schemas.py` | Pydantic 校验层：统一响应信封 + 登录/刷新/当前用户模型 |
| `app/api/routes/auth.py` | 鉴权路由：`POST /auth/login`、`POST /auth/refresh`、`GET /auth/me` |
| `app/middleware/audit.py` | 审计中间件：记录后台写操作到 `audit_logs` |
| `app/middleware/rate_limit.py` | 限流中间件：重点保护 `/auth/login` 等接口 |
| `app/main.py` | 应用入口：注册 CORS + 审计 + 限流中间件，挂载 `/auth`，提供 `/health` 与 `/` |
| `alembic/env.py` + `alembic/versions/0001_initial.py` | 迁移配置与初始建表（读取模型元数据，`create_all` 建 29 张表） |
| `seed.py` | 开发种子：创建超级管理员 + 基础权限点 + 角色（供本地联调） |
| `README.md` / `.env.example` | 运行说明与配置样例 |

## 二、前端 `frontend/`（React 18 + Vite + TS）

| 文件 | 段功能 |
| --- | --- |
| `package.json` / `vite.config.ts` / `tsconfig*.json` / `index.html` | 脚手架与构建配置（脚本：`dev` / `build` / `preview`） |
| `src/main.tsx` / `src/App.tsx` | 应用入口与根布局（挂载路由 + i18n 提供者） |
| `src/router/index.tsx` | 路由表与占位页面（路径常量集中管理，M2 起替换真实页面） |
| `src/i18n/index.ts` + `locales/zh.ts` / `locales/en.ts` | 双语基建：中英文案 key 一一对应 |
| `src/api/request.ts` | Axios 封装：自动注入 Bearer、统一错误处理 |
| `src/theme/tokens.ts` | 暗黑奢华 Design Tokens（墨黑 `#0E0E10` / 深蓝 / 香槟金 `#C2A36B`） |
| `src/styles/global.css` | 全局样式与 CSS 变量 |
| `README.md` / `.env.example` | 运行说明与配置样例 |

---

## 三、验证结果

- **后端**：venv 安装依赖成功；`import app.main` 通过；**29 张表全部注册**（表名与《数据库设计文档》一致）；FastAPI 应用实例化成功；`/auth` 三个路由已挂载；CORS / 审计 / 限流中间件已注册。
- **前端**：`npm install` 成功（101 包）；`npm run build`（tsc 类型检查 + vite 打包）通过，生成 `dist/`；中英文案 key 一一对应。
- **实时运行依赖**：登录/建表等需 MySQL 实例（本机未启 MySQL，故未做端到端运行；可用 `seed.py` + Alembic 在本地 MySQL 上联调）。

## 四、本次修复的关键问题（便于审核）

1. **SQLAlchemy 版本**：升级到 `>=2.0.36` 以兼容 Python 3.13（2.0.30 在 3.13 下有已知 enum 不兼容）。
2. **i18n 翻译表主键**：原 `models.py` 的 7 张 `*_i18n` 表只声明 `UniqueConstraint` 未声明主键，SQLAlchemy 无法映射；已补 `(实体id, lang)` 联合主键并移除冗余唯一约束。
3. **列类型**：`Decimal` 非 SQLAlchemy 顶层导出，改用语义等价的 `Numeric`（对应文档 `DECIMAL`）。
4. **关联表 `id` 主键**：`role_permissions` / `model_dealer` 的 DDL 声明了 `PRIMARY KEY(id)` 但遗漏 `id` 列，已在 ORM 中补齐。

> 注：`backend/fix_i18n_pk.py` 与 `backend/fix_pk_order.py` 为上述修正的一次性脚本，可酌情删除（不影响运行）。

## 五、待确认

请确认 M1 代码与验证结果无误。回复 **「已确认，执行下一步」** 后，我将开始 **M2 前台核心展示**。

### M2 预告
- 后端：`/brands`、`/models`（列表/详情，含筛选分页与 i18n 读取）、`/articles`、`/banners`，并加查询缓存。
- 前端：首页（Hero / Banner / 品牌墙 / 推荐）、车型列表（筛选/分页）、车型详情、资讯列表/详情，替换 M1 占位路由。
